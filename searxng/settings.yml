use_default_settings: true

server:
  secret_key: "ufo-atlas-local-searxng-secret"
  limiter: false
  image_proxy: true

search:
  safe_search: 1
  autocomplete: ""
  default_lang: "en-US"
  formats:
    - html
    - json
